<h2>Media: <%= if is_nil(@blog), do: "Latest", else: @blog.name %></h2>

<%= for {song, i} <- Enum.with_index(@songs) do %>
  <%= render MediaRadarWeb.SongView, "_song.html", song: song, blog: @blog, conn: @conn, song_counter: i %>
<% end %>
